

Note til selv:
De der highways med en masse refs, der er alle ref's bare noder der ligger på den vej.

Skal måske finde kryds ved at kigge på veje der har samme noder.

Delete driveways

Ting vi har taget hånd om, når vi laver vores noder/Grapher:
- Oneway både oneway tag, men også highway/roundabout, ved at tjekke tag, og dens attribut.
                    isOneway = child.find('./tag[@k="oneway"]') is not None and child.find('./tag[@k="oneway"]').attrib['v'] == 'yes'
                    isMotorWay = child.find('./tag[@k="highway"]') is not None and child.find('./tag[@k="highway"]').attrib['v'] == 'motorway'
                    isRoundAbout = child.find('./tag[@k="junction"]') is not None and child.find('./tag[@k="junction"]').attrib['v'] == 'roundabout'

- Driveways

Se wiki side for ting vi ikke skal have med (Nok først til malta):
https://wiki.openstreetmap.org/wiki/Key:highway
Pedestrian, Access*, Busway, (Escape mby), Bus_guideway, footway, bridleway, corridor, via_ferrata, cycleway, proposed, construction,
(Har kun skrevet de tags fra highways vi ikke skal have med, vi skal nok også ekskludere tags som alle typer af cycleway)



hvad vi kiggede på sidst:
    Der er en fejl ved9 1559838261, da vi rammer en node der har 3 edges, men ligger i midten af en ref liste.